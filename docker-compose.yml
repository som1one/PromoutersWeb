services:
  db:
    image: postgres:16-alpine
    container_name: promouters-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-promouters}
      POSTGRES_USER: ${POSTGRES_USER:-promouters}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-promouters}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-promouters} -d ${POSTGRES_DB:-promouters}"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
    container_name: promouters-backend
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "8000:8000"
    command: >
      sh -c "alembic upgrade head &&
             uvicorn promouters.main:app --host 0.0.0.0 --port 8000"

volumes:
  postgres_data:

